export enum MainEndpoints {
  GET_COUNTRIES_ENDPOINT = '/api/v1/country?page={page}&limit={limit}',
  GET_UNIVERSITIES_ENDPOINT = '/api/v1/university?page={page}&limit={limit}',
  UNIVERSITY_ENDPOINT = '/api/v1/university',
}
